import React from 'react';
import styled from 'styled-components';

const DaySelector = () => {
  return <Wrapper>DaySelector</Wrapper>;
};

const Wrapper = styled.div`
  background-color: red;
`;

export default DaySelector;
